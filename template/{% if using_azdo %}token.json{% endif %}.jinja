{
    "about": "This file is used to securely store the Azure DevOps token during the template building process to avoid possibly leaking it into command line output. It is deleted at the end of the template creation process and is not committed to Git.",
    "token": "{{ azdo_token }}"
}

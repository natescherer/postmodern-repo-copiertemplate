# Changes here will be overwritten by Copier; NEVER EDIT MANUALLY
{#- project_initialized is injected here (not a real question) so it's absent on the very first
   `copier copy` but present on every `copier update` after. copier.yml.jinja's questions check
   it to swap their help text to "Locked value on update, press Enter." once it's set. #}
{{ dict(_copier_answers, copyright_year=copyright_year, project_initialized=True) | to_nice_yaml -}}
